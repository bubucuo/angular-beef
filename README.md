##

from: https://angular.dev/tutorials/first-app/01-hello-world

start: `ng serve`

install ng: `pnpm install -g @angular/cli@latest` or `npm install -g @angular/cli@latest`

create component named details: `ng generate component details`
